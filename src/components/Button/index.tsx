import React from "react";
import { HtmlHTMLAttributes } from "react";
// import { Container } from './styles';

// TODO: button for paginate list
const Button: React.FC<React.HTMLProps<HTMLButtonElement>> = () => {
  return <Button />;
};

export default Button;
